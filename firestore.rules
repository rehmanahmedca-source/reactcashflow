rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permit read and write to all finance collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
